# LLM-Town

## Overview

LLM-Town is a multi-agent town simulation powered by Large Language Models (LLMs). The goal is to create a persistent virtual town populated by autonomous agents that possess memories, relationships, needs, goals, occupations, and daily routines.

Agents interact with one another through natural language conversations generated by an LLM, forming relationships that evolve over time. The long-term vision is to create an emergent social simulation where stories, friendships, rivalries, rumors, conflicts, and larger town-level events arise naturally from agent behavior.

The project is intended as both:

1. A software engineering project exploring multi-agent system design.
2. A research-oriented experiment in emergent behavior and LLM-powered simulations.

---

# Current Status

Current Development Phase:

**Phase 1: Core Simulation Infrastructure**

The project currently supports:

- Persistent agents
- Agent memory systems
- Agent relationships
- Agent needs
- Daily town events
- Activity planning
- LLM-generated conversations
- Action inference
- Relationship updates
- State saving/loading
- Simulation reporting
- Unit testing infrastructure

The project has successfully transitioned from a simple proof-of-concept into a functioning simulation framework.

---

# Core Concepts

## Agents

Each agent possesses:

- Name
- Personality
- Occupation
- Goals
- Needs
- Memories
- Relationships
- Directional relationship profiles and bounded social episodes
- Current activity
- Recent topics

Agents act independently and make decisions based on their internal state.

---

## Needs

Agents currently track:

### Social

Desire for interaction and relationships.

### Wealth

Desire for economic improvement and opportunity.

### Knowledge

Desire to learn information and acquire useful skills.

Needs decay over time and influence activity selection.

---

## Relationships

Relationships are represented by a numerical score:

```text
-10 -------------------- +10
```

Relationship categories:

| Score Range | Label |
|------------|---------|
| -10 to -7 | Enemies |
| -6 to -3 | Tense |
| -2 to +2 | Neutral |
| +3 to +6 | Friendly |
| +7 to +10 | Close Friends |

Relationships influence:

- Conversation frequency
- Available actions
- Conversation tone
- Future interactions

Prompt 5 adds private per-counterpart experience without replacing the legacy
pair score. Trust, affinity, cooperation, helpfulness, and hostility are bounded
and updated only from finalized structured actions/outcomes. Up to eight recent
structured episodes per counterpart condition target, strategy, intent, and
social-action policy. These private views remain separate from global reputation.
See [the Prompt 5 design and evaluation guide](docs/prompt5_relationship_memory.md).

---

## Activities

Agents choose activities based on:

- Occupation
- Goals
- Needs
- Daily events

Examples:

- Socialize
- Investigate stories
- Organize community support
- Review records
- Attend events
- Network
- Pursue business opportunities

---

## Daily Events

The town generates a daily event such as:

- Public Debate
- Town Hall Meeting
- Poetry Night
- Art Workshop

Events influence:

- Agent activity selection
- Conversations
- Memories

---

## Conversations

Agents generate conversations using an LLM.

Conversation context includes:

- Relationship status
- Memories
- Occupation
- Current activity
- Goals
- Needs
- Daily event

The LLM returns:

```json
{
  "dialogue": "...",
  "action": "chat"
}
```

Actions are parsed and used to update simulation state.

---

## Actions

Current action system:

- chat
- compliment
- apologize
- offer_help
- ask_for_help
- argue
- insult
- storm_off
- confess_feelings
- share_rumor
- cooperate

Actions affect:

- Relationships
- Needs
- Future interactions

---

## Memory System

Agents store memories of:

- Conversations
- Town events

Each memory contains:

- Day
- Hour
- Participants
- Location
- Sentiment
- Importance
- Tags

Relevant memories are injected into future conversations.

---

## Persistence

Simulation state can be saved and loaded.

Persisted information:

- Agent memories
- Relationships
- Goals
- Needs
- Activities
- Topics

This allows long-running simulations.

---

# Current Project Structure

```text
src/
│
├── actions/
├── agents/
├── analysis/
├── behavior/
├── llm/
├── simulation/
├── town/
└── utils/

tests/

data/

logs/
```

---

# Running The Simulation

## Fake LLM Mode

Fast testing mode:

```bash
python main.py --fake-llm --days 2 --hours 8 12 18 22
```

---

## Real LLM Mode

Run using a local transformer model:

```bash
python main.py --days 5 --hours 8 12 18 22
```

---

## Custom Configuration

```bash
python main.py \
    --days 50 \
    --hours 8 12 18 22 \
    --seed 42
```

---

# Reproducible Benchmarks

Run the same configuration across several seeds with the fake LLM:

```bash
python scripts/benchmark_simulation.py \
    --fake-llm \
    --days 30 \
    --seeds 1 2 3 4 5
```

Each invocation creates a new directory under `outputs/benchmarks/`. Every
seed gets isolated state, event logs, conversation logs, town-arc logs, and a
captured simulation transcript. The suite-level `benchmark.json` records the
Git revision, complete run configuration, input-file hashes, per-seed metrics,
and cross-seed aggregates. Benchmark runs never load, clear, or write the
ordinary `data/save_state.json` and `logs/` paths.

Use an explicit new destination when a stable experiment name is useful:

```bash
python scripts/benchmark_simulation.py \
    --fake-llm --days 30 --seeds 1 2 3 4 5 \
    --output-dir outputs/benchmarks/baseline-30-day
```

Existing output directories are rejected to prevent accidental mixing or
overwriting. To compare a change against that baseline, run the same days,
hours, seeds, LLM mode, and inputs:

```bash
python scripts/benchmark_simulation.py \
    --fake-llm --days 30 --seeds 1 2 3 4 5 \
    --output-dir outputs/benchmarks/candidate-30-day \
    --compare-to outputs/benchmarks/baseline-30-day
```

The comparison writes `comparison.json`. Its verdict is based on quality-check
pass-rate transitions across matching seeds; metric changes that do not cross
a quality threshold are reported as `different`, not automatically better or
worse. Mixed improvements and regressions are reported as `mixed`, and
configuration mismatches as `incomparable`.

The quality report uses the same metric definitions and can inspect either an
ordinary run or one benchmark seed:

```bash
python scripts/quality_report.py
python scripts/quality_report.py \
    --run-dir outputs/benchmarks/baseline-30-day/seed-1
python scripts/quality_report.py \
    --run-dir outputs/benchmarks/baseline-30-day/seed-1 --json
```

## Real-LLM evaluation

Use the deterministic fake-LLM benchmark above for simulation regression
testing. Its exact-dialogue repetition reflects intentionally fixed fake
responses and is not a real-model quality measurement.

Evaluate real dialogue separately with:

```bash
python scripts/evaluate_real_llm.py --days 10 --seed 42
```

Each run creates a new directory under `outputs/real_llm_evaluations/` and
rejects an existing destination. It uses isolated state and logs, leaving
`data/save_state.json`, ordinary logs, benchmarks, and prior evaluations
untouched. The top-level `metadata.json`, `metrics.json`, `transcript.txt`, and
`review.md` separate reproducibility metadata, measurable behavior, full text,
and compact heuristic review samples. Context-use labels are lexical indicators,
not a subjective quality score or proof of causality.

---

# Testing

Run all tests:

```bash
pytest
```

Current test coverage includes:

- Relationship system
- Conversation parser

Additional tests will be added as development continues.

---

# Immediate Next Features

These are the highest-priority features currently planned.

## 1. Relationship Dynamics Improvements

Prompt 5 now provides bounded, directional shared-history conditioning.
Possible later improvements include personality compatibility and more explicit
multi-turn outcome modeling without expanding the current lightweight policy.

---

## 2. Daily Agent Journals

Agents will summarize important events from each day.

Example:

```text
Day 14 Summary:

- Attended the Town Hall Meeting
- Helped Ethan organize a fundraiser
- Relationship with Ethan improved
```

This will support long-term memory compression.

---

## 3. Expanded Testing

Planned tests:

- Need system
- Memory retrieval
- Activity planner
- Action system
- Save/load functionality

---

## 4. Simulation Metrics

Additional analytics:

- Relationship distribution
- Agent social networks
- Most influential agents
- Activity distributions
- Conversation statistics

---

# Long-Term Roadmap

## Phase 2: Social Simulation

- Friendship formation
- Rivalries
- Rumor propagation
- Trust systems
- Reputation systems

---

## Phase 3: Goal-Driven Behavior

Agents generate new goals dynamically.

Examples:

- Start a business
- Make friends
- Investigate rumors
- Earn money
- Improve reputation

---

## Phase 4: Town Economy

Potential additions:

- Businesses
- Employment
- Trade
- Wealth accumulation
- Economic events

---

## Phase 5: Emergent Narrative

The simulation should begin generating stories such as:

- Political conflicts
- Business rivalries
- Friend groups
- Community projects
- Scandals
- Personal dramas

without explicit scripting.

---

## Phase 6: Crime and Justice

Possible future systems:

- Theft
- Fraud
- Investigation
- Witnesses
- Law enforcement
- Courts

---

## Phase 7: Large-Scale Simulation

Future goals:

- Hundreds of agents
- Long-running simulations
- Better memory retrieval
- Efficient local LLM inference
- Agent clustering
- Event chains

---

# Research Questions

The project is also intended to explore:

- Can believable social behavior emerge from simple rules + LLM dialogue?
- How much memory is necessary for coherent long-term agents?
- How should relationships evolve over time?
- What structures are needed for emergent storytelling?
- Can local LLMs support persistent virtual societies?

---

# Current Limitations

Known limitations:

- Small agent population
- Simple relationship dynamics
- Limited memory retrieval
- No economy
- No reputation system
- No long-term planning
- No town-wide consequences
- Limited action set

These limitations are expected at the current development stage.

---

# Author

Created as a personal AI engineering project focused on:

- Multi-agent systems
- Simulation design
- LLM integration
- Emergent behavior
- Long-term agent memory
- Autonomous social environments
